version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    container_name: portfolio-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
